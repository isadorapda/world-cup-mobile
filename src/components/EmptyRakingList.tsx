import { Text } from 'native-base'

export function EmptyRakingList() {
  return (
    <Text color="white" fontSize="sm" textAlign="center">
      Wait for the match's result to get this poll's ranking
    </Text>
  )
}
